name: JavaGoat-WhatsApp-Bot on:
workflow_dispatch: schedule:
- cron: "0 */5"***
Jobs: build: runs-on: ubuntu-latest steps:
- name: Checkout Code
uses: actions/checkout@v4
- name: Setup Node.js
uses: actions/setup-node@v4 with:
node-version: 20
- ran: nminstallpendencies
- name: Start Al Bot env:
FIREBASE_URL: SIf secrets.FIREBASE_URL H
run: npm start
